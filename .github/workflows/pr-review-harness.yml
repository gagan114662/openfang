name: pr-review-harness

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:
    inputs:
      pr_number:
        description: "PR number for trusted maintainer rerun"
        required: true
        type: string

permissions:
  contents: read
  pull-requests: write
  issues: write
  checks: read
  actions: read

concurrency:
  group: pr-review-harness-${{ github.event.pull_request.number || inputs.pr_number || github.run_id }}
  cancel-in-progress: true

jobs:
  resolve-context:
    name: resolve-context
    runs-on: ubuntu-latest
    outputs:
      pr_number: ${{ steps.ctx.outputs.pr_number }}
      head_sha: ${{ steps.ctx.outputs.head_sha }}
      head_repo: ${{ steps.ctx.outputs.head_repo }}
      head_ref: ${{ steps.ctx.outputs.head_ref }}
      is_fork: ${{ steps.ctx.outputs.is_fork }}
      trusted_rerun: ${{ steps.ctx.outputs.trusted_rerun }}
      should_block_fork: ${{ steps.ctx.outputs.should_block_fork }}
    steps:
      - name: Resolve PR context
        id: ctx
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = require('path');
            const [owner, repo] = process.env.GITHUB_REPOSITORY.split('/');
            const eventName = context.eventName;

            let pr;
            let trustedRerun = false;
            if (eventName === 'pull_request') {
              pr = context.payload.pull_request;
            } else if (eventName === 'workflow_dispatch') {
              const inputPr = Number(core.getInput('pr_number', { required: true }));
              const actor = context.actor;
              const perm = await github.rest.repos.getCollaboratorPermissionLevel({
                owner,
                repo,
                username: actor,
              });
              const level = String(perm.data.permission || '');
              const allowed = ['admin', 'maintain', 'write'].includes(level);
              if (!allowed) {
                core.setFailed(`workflow_dispatch trusted rerun denied for actor=${actor} permission=${level}`);
                return;
              }
              trustedRerun = true;
              const prResp = await github.rest.pulls.get({
                owner,
                repo,
                pull_number: inputPr,
              });
              pr = prResp.data;
            } else {
              core.setFailed(`Unsupported event: ${eventName}`);
              return;
            }

            const prNumber = Number(pr.number);
            const headSha = String(pr.head.sha);
            const headRepo = String(pr.head.repo.full_name);
            const headRef = String(pr.head.ref);
            const isFork = headRepo !== `${owner}/${repo}`;
            const shouldBlockFork = isFork && !trustedRerun;

            const files = await github.paginate(github.rest.pulls.listFiles, {
              owner,
              repo,
              pull_number: prNumber,
              per_page: 100,
            });
            const changed = files.map((item) => item.filename);
            fs.mkdirSync('artifacts/context', { recursive: true });
            fs.writeFileSync('artifacts/context/changed_files.txt', changed.join('\n') + '\n');
            fs.writeFileSync(
              'artifacts/context/context.json',
              JSON.stringify(
                {
                  pr_number: prNumber,
                  head_sha: headSha,
                  head_repo: headRepo,
                  head_ref: headRef,
                  is_fork: isFork,
                  trusted_rerun: trustedRerun,
                  should_block_fork: shouldBlockFork,
                  changed_files: changed,
                },
                null,
                2
              ) + '\n'
            );

            core.setOutput('pr_number', String(prNumber));
            core.setOutput('head_sha', headSha);
            core.setOutput('head_repo', headRepo);
            core.setOutput('head_ref', headRef);
            core.setOutput('is_fork', String(isFork));
            core.setOutput('trusted_rerun', String(trustedRerun));
            core.setOutput('should_block_fork', String(shouldBlockFork));

      - name: Upload context artifact
        uses: actions/upload-artifact@v4
        with:
          name: pr-review-context
          path: artifacts/context/
          if-no-files-found: error

  collect-evidence:
    name: collect-evidence
    needs: resolve-context
    if: ${{ needs.resolve-context.outputs.should_block_fork != 'true' }}
    runs-on: ubuntu-latest
    outputs:
      preflight_exit_code: ${{ steps.preflight.outputs.exit_code }}
      live_exit_code: ${{ steps.live.outputs.exit_code }}
      gate_exit_code: ${{ steps.gate.outputs.exit_code }}
      eval_exit_code: ${{ steps.evals.outputs.exit_code }}
      evidence_exit_code: ${{ steps.verify.outputs.exit_code }}
      sentry_required: ${{ steps.required_checks.outputs.require_sentry }}
      sentry_exit_code: ${{ steps.sentry_validate.outputs.exit_code || '0' }}
      ui_impact: ${{ steps.ui.outputs.ui_impact }}
    steps:
      - uses: actions/checkout@v4
        with:
          repository: ${{ needs.resolve-context.outputs.head_repo }}
          ref: ${{ needs.resolve-context.outputs.head_ref }}
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install Python dependencies
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install pillow

      - name: Run infra preflight
        id: preflight
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mkdir -p artifacts
          set +e
          python3 scripts/harness/infra_preflight.py \
            --contract .harness/policy.contract.json \
            --workflow pr-review-harness \
            --repo "${{ github.repository }}" \
            --token-env GITHUB_TOKEN \
            --out artifacts/infra-preflight-report.json
          code=$?
          set -e
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"

      - name: Upsert infra incident
        if: always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python3 scripts/harness/infra_incident_upsert.py \
            --repo "${{ github.repository }}" \
            --token-env GITHUB_TOKEN \
            --contract .harness/policy.contract.json \
            --report artifacts/infra-preflight-report.json \
            --out artifacts/infra-incident-upsert.json

      - name: Enforce infra preflight
        run: |
          if [ "${{ steps.preflight.outputs.exit_code }}" != "0" ]; then
            echo "Infra preflight failed."
            exit 1
          fi

      - name: Download context artifact
        uses: actions/download-artifact@v4
        with:
          name: pr-review-context
          path: .

      - name: Normalize context artifact paths
        run: |
          set -euo pipefail
          mkdir -p artifacts/context
          changed_file=$(find . -type f -name changed_files.txt | head -1 || true)
          context_file=$(find . -type f -name context.json | head -1 || true)
          if [ -z "$changed_file" ]; then
            echo "changed_files.txt not found after artifact download"
            exit 1
          fi
          cp "$changed_file" artifacts/context/changed_files.txt
          if [ -n "$context_file" ]; then
            cp "$context_file" artifacts/context/context.json
          fi

      - name: Install media dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg jq

      - name: Compute risk tier
        id: risk_tier
        run: |
          PYTHONPATH=scripts/harness python3 - <<'PY'
          from checks_resolver import compute_risk_tier, load_contract, read_changed_files
          contract = load_contract(".harness/policy.contract.json")
          changed = read_changed_files("artifacts/context/changed_files.txt")
          tier = compute_risk_tier(changed, contract)
          with open("/tmp/risk_tier.out", "w", encoding="utf-8") as f:
              f.write(tier)
          PY
          echo "risk_tier=$(cat /tmp/risk_tier.out)" >> "$GITHUB_OUTPUT"

      - name: Determine required check coverage
        id: required_checks
        run: |
          PYTHONPATH=scripts/harness python3 - <<'PY'
          from checks_resolver import compute_required_checks, load_contract

          contract = load_contract(".harness/policy.contract.json")
          tier = "${{ steps.risk_tier.outputs.risk_tier }}"
          required = compute_required_checks(contract, tier)
          require_sentry = "sentry-live-validate" in set(required)
          with open("/tmp/required_checks.out", "w", encoding="utf-8") as f:
              f.write(f"require_sentry={'true' if require_sentry else 'false'}\n")
          PY
          cat /tmp/required_checks.out >> "$GITHUB_OUTPUT"

      - name: Run live provider probe
        id: live
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          mkdir -p artifacts/agent-evals
          set +e
          python3 scripts/harness/live_provider_probe.py \
            --contract .harness/policy.contract.json \
            --head-sha "${{ needs.resolve-context.outputs.head_sha }}" \
            --risk-tier "${{ steps.risk_tier.outputs.risk_tier }}" \
            --out artifacts/agent-evals/live-provider-report.json
          code=$?
          set -e
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"

      - name: Detect UI impact
        id: ui
        run: |
          PYTHONPATH=scripts/harness python3 - <<'PY'
          from checks_resolver import load_contract, read_changed_files, requires_browser_evidence
          contract = load_contract(".harness/policy.contract.json")
          changed = read_changed_files("artifacts/context/changed_files.txt")
          ui = requires_browser_evidence(changed, contract)
          with open("/tmp/ui_impact.out", "w", encoding="utf-8") as f:
              f.write("true" if ui else "false")
          PY
          echo "ui_impact=$(cat /tmp/ui_impact.out)" >> "$GITHUB_OUTPUT"

      - name: Set up Node for optional Playwright
        if: ${{ steps.ui.outputs.ui_impact == 'true' }}
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Optional Playwright capture
        if: ${{ steps.ui.outputs.ui_impact == 'true' && vars.OPENFANG_ENABLE_PLAYWRIGHT == '1' }}
        env:
          OPENFANG_ENABLE_PLAYWRIGHT: ${{ vars.OPENFANG_ENABLE_PLAYWRIGHT }}
          OPENFANG_UI_BASE_URL: ${{ vars.OPENFANG_UI_BASE_URL }}
          OPENFANG_PLAYWRIGHT_ROUTES: ${{ vars.OPENFANG_PLAYWRIGHT_ROUTES }}
          OPENFANG_UI_IMPACT: ${{ steps.ui.outputs.ui_impact }}
          PLAYWRIGHT_RESULT_OUT: artifacts/playwright-evidence.json
          PLAYWRIGHT_MANIFEST: artifacts/browser-evidence-manifest.json
          PLAYWRIGHT_EVIDENCE_DIR: artifacts/pr-review/evidence
        run: |
          npm --version
          npx -y playwright@1.52.0 install chromium || true
          node scripts/harness/playwright_capture.mjs || true

      - name: Generate deterministic evidence package
        run: |
          python3 scripts/harness/evidence_capture.py \
            --head-sha "${{ needs.resolve-context.outputs.head_sha }}" \
            --changed-files artifacts/context/changed_files.txt \
            --risk-report artifacts/risk-policy-report.json \
            --out-dir artifacts/pr-review/evidence \
            --out-manifest artifacts/browser-evidence-manifest.json \
            --ui-impact "${{ steps.ui.outputs.ui_impact }}" \
            --playwright-json artifacts/playwright-evidence.json

      - name: Ingest Claude feedback
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set +e
          python3 scripts/harness/claude_feedback_ingest.py \
            --repo "${{ github.repository }}" \
            --pr "${{ needs.resolve-context.outputs.pr_number }}" \
            --head-sha "${{ needs.resolve-context.outputs.head_sha }}" \
            --contract .harness/policy.contract.json \
            --token-env GITHUB_TOKEN \
            --out artifacts/claude-findings.json
          code=$?
          set -e
          echo "claude_ingest_exit_code=$code"

      - name: Run risk policy gate
        id: gate
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set +e
          python3 scripts/harness/risk_policy_gate.py \
            --pr "${{ needs.resolve-context.outputs.pr_number }}" \
            --head-sha "${{ needs.resolve-context.outputs.head_sha }}" \
            --changed-files artifacts/context/changed_files.txt \
            --repo "${{ github.repository }}" \
            --review-findings artifacts/review-findings.json \
            --claude-findings artifacts/claude-findings.json \
            --infra-preflight-report artifacts/infra-preflight-report.json \
            --live-provider-report artifacts/agent-evals/live-provider-report.json \
            --browser-evidence-manifest artifacts/browser-evidence-manifest.json \
            --report-out artifacts/risk-policy-report.json
          code=$?
          set -e
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"

      - name: Run Sentry live validation
        id: sentry_validate
        if: ${{ steps.required_checks.outputs.require_sentry == 'true' }}
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          OPENFANG_SENTRY_AUTH_TOKEN: ${{ secrets.OPENFANG_SENTRY_AUTH_TOKEN }}
          SENTRY_ORG_SLUG: ${{ vars.SENTRY_ORG_SLUG }}
          OPENFANG_SENTRY_ORG: ${{ vars.OPENFANG_SENTRY_ORG }}
          SENTRY_PROJECT_SLUG: ${{ vars.SENTRY_PROJECT_SLUG }}
          OPENFANG_SENTRY_PROJECT: ${{ vars.OPENFANG_SENTRY_PROJECT }}
          OPENFANG_API_BASE_URL: ${{ vars.OPENFANG_API_BASE_URL }}
        run: |
          mkdir -p artifacts
          export SENTRY_AUTH_TOKEN="${SENTRY_AUTH_TOKEN:-$OPENFANG_SENTRY_AUTH_TOKEN}"
          org="${SENTRY_ORG_SLUG:-$OPENFANG_SENTRY_ORG}"
          project="${SENTRY_PROJECT_SLUG:-$OPENFANG_SENTRY_PROJECT}"
          api_base="${OPENFANG_API_BASE_URL:-}"

          if [ -z "$api_base" ] || [ -z "$org" ] || [ -z "$project" ] || [ -z "${SENTRY_AUTH_TOKEN:-}" ]; then
            python3 - <<'PY'
          import json
          from pathlib import Path

          payload = {
              "status": "error",
              "errors": [
                  "missing OPENFANG_API_BASE_URL or Sentry org/project/auth config in workflow env",
              ],
          }
          Path("artifacts/sentry-logs-validation.json").write_text(
              json.dumps(payload, indent=2, sort_keys=True) + "\n",
              encoding="utf-8",
          )
          PY
            echo "exit_code=1" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          set +e
          python3 scripts/harness/sentry_logs_live_validate.py \
            --api-base "$api_base" \
            --org "$org" \
            --project "$project" \
            --token-env "SENTRY_AUTH_TOKEN" \
            --out artifacts/sentry-logs-validation.json
          code=$?
          set -e
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"

      - name: Refresh evidence package with risk report
        run: |
          python3 scripts/harness/evidence_capture.py \
            --head-sha "${{ needs.resolve-context.outputs.head_sha }}" \
            --changed-files artifacts/context/changed_files.txt \
            --risk-report artifacts/risk-policy-report.json \
            --out-dir artifacts/pr-review/evidence \
            --out-manifest artifacts/browser-evidence-manifest.json \
            --ui-impact "${{ steps.ui.outputs.ui_impact }}" \
            --playwright-json artifacts/playwright-evidence.json

      - name: Run deterministic blocking agent evals
        id: evals
        run: |
          set +e
          python3 scripts/harness/agent_eval_runner.py \
            --scenarios .harness/evals/scenarios.blocking.json \
            --contract .harness/policy.contract.json \
            --head-sha "${{ needs.resolve-context.outputs.head_sha }}" \
            --profile blocking \
            --seed 42 \
            --changed-files artifacts/context/changed_files.txt \
            --out-dir artifacts/agent-evals \
            --enforce true
          code=$?
          set -e
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"

      - name: Emit eval metrics (advisory telemetry)
        if: always()
        env:
          OPENFANG_EVAL_EMIT_SENTRY: ${{ vars.OPENFANG_EVAL_EMIT_SENTRY || 'false' }}
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
        run: |
          if [ -f artifacts/agent-evals/eval-results.json ]; then
            python3 scripts/harness/eval_metrics_emit.py \
              --eval-results artifacts/agent-evals/eval-results.json \
              --out artifacts/agent-evals/eval-metrics.json \
              --emit-sentry "${OPENFANG_EVAL_EMIT_SENTRY}"
          else
            echo "eval-results.json missing; skipping metrics emit"
          fi

      - name: Verify evidence manifest
        id: verify
        run: |
          set +e
          cmd=(python3 scripts/harness/browser_evidence_verify.py \
            --manifest artifacts/browser-evidence-manifest.json \
            --head-sha "${{ needs.resolve-context.outputs.head_sha }}" \
            --min-screenshots 2 \
            --min-videos 1)

          if [ "${{ steps.ui.outputs.ui_impact }}" = "true" ]; then
            cmd+=(--required-assertion ui_screenshot_evidence)
            cmd+=(--required-assertion ui_video_evidence)
          fi

          "${cmd[@]}"
          code=$?
          set -e
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"

      - name: Upload PR review evidence artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pr-review-evidence
          path: |
            artifacts/context/changed_files.txt
            artifacts/risk-policy-report.json
            artifacts/infra-preflight-report.json
            artifacts/infra-incident-upsert.json
            artifacts/review-findings.json
            artifacts/claude-findings.json
            artifacts/agent-evals/live-provider-report.json
            artifacts/agent-evals/eval-results.json
            artifacts/agent-evals/eval-findings.json
            artifacts/agent-evals/eval-summary.md
            artifacts/agent-evals/eval-metrics.json
            artifacts/sentry-logs-validation.json
            artifacts/browser-evidence-manifest.json
            artifacts/playwright-evidence.json
            artifacts/pr-review/evidence/
          if-no-files-found: warn

  generate-packet:
    name: generate-packet
    needs: [resolve-context, collect-evidence]
    if: ${{ needs.resolve-context.outputs.should_block_fork != 'true' }}
    runs-on: ubuntu-latest
    outputs:
      packet_exit_code: ${{ steps.packet.outputs.exit_code }}
      checklist_all_pass: ${{ steps.packet.outputs.all_pass }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Download PR review evidence artifact
        uses: actions/download-artifact@v4
        with:
          name: pr-review-evidence
          path: .

      - name: Normalize evidence artifact paths
        run: |
          set -euo pipefail
          mkdir -p artifacts/context artifacts/pr-review artifacts/pr-review/evidence artifacts/agent-evals
          changed_file=$(find . -type f -name changed_files.txt | head -1 || true)
          risk_report=$(find . -type f -name risk-policy-report.json | head -1 || true)
          infra_report=$(find . -type f -name infra-preflight-report.json | head -1 || true)
          live_report=$(find . -type f -name live-provider-report.json | head -1 || true)
          sentry_report=$(find . -type f -name sentry-logs-validation.json | head -1 || true)
          evidence_manifest=$(find . -type f -name browser-evidence-manifest.json | head -1 || true)
          eval_results=$(find . -type f -name eval-results.json | head -1 || true)
          claude_findings=$(find . -type f -name claude-findings.json | head -1 || true)
          if [ -z "$changed_file" ] || [ -z "$evidence_manifest" ]; then
            echo "required evidence artifacts not found after download"
            exit 1
          fi
          cp "$changed_file" artifacts/context/changed_files.txt
          if [ -n "$risk_report" ]; then
            cp "$risk_report" artifacts/risk-policy-report.json
          fi
          if [ -n "$infra_report" ]; then
            cp "$infra_report" artifacts/infra-preflight-report.json
          fi
          if [ -n "$live_report" ]; then
            cp "$live_report" artifacts/agent-evals/live-provider-report.json
          fi
          if [ -n "$sentry_report" ]; then
            cp "$sentry_report" artifacts/sentry-logs-validation.json
          fi
          if [ -n "$claude_findings" ]; then
            src_claude="$(realpath "$claude_findings")"
            dst_claude="$(realpath -m artifacts/claude-findings.json)"
            if [ "$src_claude" != "$dst_claude" ]; then
              cp "$claude_findings" artifacts/claude-findings.json
            fi
          fi
          cp "$evidence_manifest" artifacts/browser-evidence-manifest.json
          if [ -n "$eval_results" ]; then
            cp "$eval_results" artifacts/agent-evals/eval-results.json
          fi

      - name: Build PR packet
        id: packet
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set +e
          python3 scripts/harness/pr_packet.py \
            --acceptance-model .harness/acceptance.criteria.json \
            --contract .harness/policy.contract.json \
            --changed-files artifacts/context/changed_files.txt \
            --risk-report artifacts/risk-policy-report.json \
            --infra-preflight-report artifacts/infra-preflight-report.json \
            --live-provider-report artifacts/agent-evals/live-provider-report.json \
            --eval-results artifacts/agent-evals/eval-results.json \
            --claude-findings artifacts/claude-findings.json \
            --sentry-validation-report artifacts/sentry-logs-validation.json \
            --evidence-manifest artifacts/browser-evidence-manifest.json \
            --head-sha "${{ needs.resolve-context.outputs.head_sha }}" \
            --repo "${{ github.repository }}" \
            --pr-number "${{ needs.resolve-context.outputs.pr_number }}" \
            --out-dir artifacts/pr-review
          code=$?
          set -e
          all_pass=$(jq -r '.all_passed' artifacts/pr-review/acceptance-checklist.json 2>/dev/null || echo "false")
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          echo "all_pass=$all_pass" >> "$GITHUB_OUTPUT"

      - name: Upload PR packet artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pr-review-packet
          path: artifacts/pr-review/
          if-no-files-found: warn

  publish:
    name: publish
    needs: [resolve-context, collect-evidence, generate-packet]
    if: always()
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Download PR review packet artifact
        if: ${{ needs.resolve-context.outputs.should_block_fork != 'true' }}
        uses: actions/download-artifact@v4
        with:
          name: pr-review-packet
          path: .

      - name: Download PR review evidence artifact
        if: ${{ needs.resolve-context.outputs.should_block_fork != 'true' }}
        uses: actions/download-artifact@v4
        with:
          name: pr-review-evidence
          path: .

      - name: Normalize PR packet paths
        if: ${{ needs.resolve-context.outputs.should_block_fork != 'true' }}
        run: |
          set -euo pipefail
          mkdir -p artifacts/pr-review
          body_file=$(find . -type f -name pr-body-block.md | head -1 || true)
          comment_file=$(find . -type f -name pr-comment.md | head -1 || true)
          if [ -z "$body_file" ] || [ -z "$comment_file" ]; then
            echo "pr packet markdown files not found after artifact download"
            exit 1
          fi
          cp "$body_file" artifacts/pr-review/pr-body-block.md
          cp "$comment_file" artifacts/pr-review/pr-comment.md

      - name: Normalize PR evidence media
        if: ${{ needs.resolve-context.outputs.should_block_fork != 'true' }}
        run: |
          set -euo pipefail
          mkdir -p artifacts/pr-review/public-media

          req_files=(
            "01-diff-summary.png"
            "02-verification-summary.png"
            "03-checklist-preview.png"
            "00-implementation-walkthrough.mp4"
          )

          for f in "${req_files[@]}"; do
            src=$(find . -type f -name "$f" | head -1 || true)
            if [ -z "$src" ]; then
              echo "required media file missing after artifact download: $f"
              exit 1
            fi
            cp "$src" "artifacts/pr-review/public-media/$f"
          done

          optional_files=(
            "browser-evidence-manifest.json"
            "acceptance-checklist.md"
            "acceptance-checklist.json"
            "eval-summary.md"
            "eval-results.json"
          )
          for f in "${optional_files[@]}"; do
            src=$(find . -type f -name "$f" | head -1 || true)
            if [ -n "$src" ]; then
              cp "$src" "artifacts/pr-review/public-media/$f"
            fi
          done

      - name: Publish direct-view media branch
        id: media
        if: ${{ needs.resolve-context.outputs.should_block_fork != 'true' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ needs.resolve-context.outputs.pr_number }}
          RUN_ID: ${{ github.run_id }}
        run: |
          set -euo pipefail
          repo="${GITHUB_REPOSITORY}"
          branch="pr-review-evidence"
          target="_pr_review_media/pr-${PR_NUMBER}/run-${RUN_ID}"
          auth_url="https://x-access-token:${GITHUB_TOKEN}@github.com/${repo}.git"
          temp_dir=$(mktemp -d /tmp/pr-review-media-branch.XXXXXX)

          if git ls-remote --exit-code --heads "$auth_url" "$branch" >/dev/null 2>&1; then
            git clone --depth 1 --branch "$branch" "$auth_url" "$temp_dir"
          else
            git clone --depth 1 "$auth_url" "$temp_dir"
            (
              cd "$temp_dir"
              git checkout --orphan "$branch"
              git rm -rf . || true
            )
          fi

          mkdir -p "$temp_dir/$target"
          cp artifacts/pr-review/public-media/* "$temp_dir/$target/"

          (
            cd "$temp_dir"
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add "$target"
            if ! git diff --cached --quiet; then
              git commit -m "chore(harness): publish PR review media for PR #${PR_NUMBER} run ${RUN_ID}"
              git push origin "$branch"
            fi
          )

          raw_base="https://raw.githubusercontent.com/${repo}/${branch}/${target}"
          blob_base="https://github.com/${repo}/blob/${branch}/${target}"
          echo "raw_base=$raw_base" >> "$GITHUB_OUTPUT"
          echo "blob_base=$blob_base" >> "$GITHUB_OUTPUT"

      - name: Inject direct preview links
        if: ${{ needs.resolve-context.outputs.should_block_fork != 'true' }}
        env:
          RAW_BASE: ${{ steps.media.outputs.raw_base }}
          BLOB_BASE: ${{ steps.media.outputs.blob_base }}
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import os
          import re
          from pathlib import Path

          raw = os.environ["RAW_BASE"].strip()
          blob = os.environ["BLOB_BASE"].strip()
          if not raw or not blob:
              raise SystemExit("missing media base URLs")

          media_block = f"""<!-- pr-review-media:start -->
          ### Direct Media Preview
          - [Diff screenshot]({blob}/01-diff-summary.png)
          - [Verification screenshot]({blob}/02-verification-summary.png)
          - [Checklist screenshot]({blob}/03-checklist-preview.png)
          - [Walkthrough video (GitHub preview)]({blob}/00-implementation-walkthrough.mp4)
          - [Walkthrough video (direct file)]({raw}/00-implementation-walkthrough.mp4)
          - [Evidence manifest]({blob}/browser-evidence-manifest.json)
          - [Acceptance checklist]({blob}/acceptance-checklist.md)
          - [Agent eval summary]({blob}/eval-summary.md)
          - [Agent eval results]({blob}/eval-results.json)

          ![Diff screenshot]({raw}/01-diff-summary.png)
          ![Verification screenshot]({raw}/02-verification-summary.png)
          ![Checklist screenshot]({raw}/03-checklist-preview.png)
          <!-- pr-review-media:end -->"""

          media_re = re.compile(r"<!-- pr-review-media:start -->.*?<!-- pr-review-media:end -->", re.DOTALL)

          comment_path = Path("artifacts/pr-review/pr-comment.md")
          comment = comment_path.read_text(encoding="utf-8")
          if media_re.search(comment):
              comment = media_re.sub(media_block, comment)
          else:
              comment = comment.rstrip() + "\n\n" + media_block + "\n"
          comment_path.write_text(comment, encoding="utf-8")

          body_path = Path("artifacts/pr-review/pr-body-block.md")
          body = body_path.read_text(encoding="utf-8")
          if media_re.search(body):
              body = media_re.sub(media_block, body)
          else:
              end_marker = "<!-- pr-review-checklist:end -->"
              if end_marker in body:
                  body = body.replace(end_marker, media_block + "\n" + end_marker, 1)
              else:
                  body = body.rstrip() + "\n\n" + media_block + "\n"
          body_path.write_text(body, encoding="utf-8")
          PY

      - name: Write fork block packet
        if: ${{ needs.resolve-context.outputs.should_block_fork == 'true' }}
        run: |
          mkdir -p artifacts/pr-review
          cat > artifacts/pr-review/pr-body-block.md <<'EOF'
          <!-- pr-review-checklist:start -->
          ## PR Review Harness Checklist

          - Status: **BLOCKED**
          - Reason: Fork PR requires trusted maintainer rerun (`workflow_dispatch` with `pr_number`)
          <!-- pr-review-checklist:end -->
          EOF
          cat > artifacts/pr-review/pr-comment.md <<'EOF'
          <!-- pr-review-harness -->
          ### PR Review Harness

          Status: **BLOCKED**

          This PR originates from a fork and is blocked pending trusted maintainer rerun.
          Run `pr-review-harness` via `workflow_dispatch` with this `pr_number`.
          EOF

      - name: Upsert PR review block/comment
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python3 scripts/harness/pr_review_upsert.py \
            --repo "${{ github.repository }}" \
            --pr-number "${{ needs.resolve-context.outputs.pr_number }}" \
            --body-block artifacts/pr-review/pr-body-block.md \
            --comment-body artifacts/pr-review/pr-comment.md \
            --out artifacts/pr-review/upsert-result.json

      - name: Upload publish artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pr-review-publish
          path: artifacts/pr-review/upsert-result.json
          if-no-files-found: warn

  enforce:
    name: pr-review-harness
    needs: [resolve-context, collect-evidence, generate-packet, publish]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Enforce PR review harness result
        run: |
          if [ "${{ needs.resolve-context.outputs.should_block_fork }}" = "true" ]; then
            echo "Fork PR blocked pending trusted maintainer rerun."
            exit 1
          fi

          if [ "${{ needs.collect-evidence.outputs.preflight_exit_code }}" != "0" ]; then
            echo "Infra preflight failed inside PR review harness."
            exit 1
          fi

          if [ "${{ needs.collect-evidence.outputs.gate_exit_code }}" != "0" ]; then
            echo "risk-policy-gate failed inside PR review harness."
            exit 1
          fi

          if [ "${{ needs.collect-evidence.outputs.eval_exit_code }}" != "0" ]; then
            echo "Deterministic agent evals failed."
            exit 1
          fi

          if [ "${{ needs.collect-evidence.outputs.evidence_exit_code }}" != "0" ]; then
            echo "Evidence verification failed."
            exit 1
          fi

          if [ "${{ needs.collect-evidence.outputs.sentry_required }}" = "true" ] && [ "${{ needs.collect-evidence.outputs.sentry_exit_code }}" != "0" ]; then
            echo "Sentry live validation failed."
            exit 1
          fi

          if [ "${{ needs.generate-packet.outputs.packet_exit_code }}" != "0" ]; then
            echo "PR packet generation returned non-zero."
            exit 1
          fi

          if [ "${{ needs.generate-packet.outputs.checklist_all_pass }}" != "true" ]; then
            echo "Acceptance checklist did not fully pass."
            exit 1
          fi

          echo "PR review harness passed."

      - name: Build workflow telemetry inputs
        if: always()
        run: |
          mkdir -p artifacts/agent-evals
          python3 - <<'PY'
          import json
          from pathlib import Path

          preflight_fail = '${{ needs.collect-evidence.outputs.preflight_exit_code }}' != '0'
          gate_fail = '${{ needs.collect-evidence.outputs.gate_exit_code }}' != '0'
          eval_fail = '${{ needs.collect-evidence.outputs.eval_exit_code }}' != '0'

          infra = {
              "status": "fail" if preflight_fail else "pass",
              "workflow": "pr-review-harness",
          }
          Path("artifacts/infra-preflight-report.json").write_text(
              json.dumps(infra, indent=2, sort_keys=True) + "\\n",
              encoding="utf-8",
          )

          risk = {
              "decision": "fail" if gate_fail else "pass",
              "status": "fail" if gate_fail else "pass",
          }
          Path("artifacts/risk-policy-report.json").write_text(
              json.dumps(risk, indent=2, sort_keys=True) + "\\n",
              encoding="utf-8",
          )

          eval_payload = {
              "profile": "blocking",
              "summary": {
                  "failed": 1 if eval_fail else 0,
                  "all_blocking_passed": not eval_fail,
              },
          }
          Path("artifacts/agent-evals/eval-results.json").write_text(
              json.dumps(eval_payload, indent=2, sort_keys=True) + "\\n",
              encoding="utf-8",
          )
          PY

      - name: Emit workflow failure telemetry
        if: always()
        env:
          OPENFANG_SENTRY_WORKFLOW_EVENTS: ${{ vars.OPENFANG_SENTRY_WORKFLOW_EVENTS || 'true' }}
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
        run: |
          python3 scripts/harness/workflow_sentry_emit.py \
            --workflow "pr-review-harness" \
            --job "pr-review-harness" \
            --run-id "${{ github.run_id }}" \
            --run-attempt "${{ github.run_attempt }}" \
            --event "${{ github.event_name }}" \
            --repo "${{ github.repository }}" \
            --head-sha "${{ needs.resolve-context.outputs.head_sha }}" \
            --pr-number "${{ needs.resolve-context.outputs.pr_number }}" \
            --status "${{ job.status }}" \
            --emit "${OPENFANG_SENTRY_WORKFLOW_EVENTS}" \
            --emit-on-failure-only "true" \
            --sentry-dsn-env "SENTRY_DSN" \
            --infra-preflight-report artifacts/infra-preflight-report.json \
            --risk-policy-report artifacts/risk-policy-report.json \
            --eval-results artifacts/agent-evals/eval-results.json \
            --out artifacts/sentry-workflow-emit.json

      - name: Upload workflow telemetry artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pr-review-harness-sentry-telemetry
          path: artifacts/sentry-workflow-emit.json
          if-no-files-found: warn
